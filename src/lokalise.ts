import { IConfig } from './models/IConfig';

export const Lokalise: IConfig = {
  apiKey: 'your-api-key',
  projectId: 'your-project-id',
  referenceLanguageIso: 'en',
  url: 'https://api.lokalise.com/your-url',
};
